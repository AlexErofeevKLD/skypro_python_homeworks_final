Тест #1 на заглавные буквы: 
    При проведении автотеста, Pytest выдал ошибку 
    в не соответствии названии функции (def capitilize) и методе ответа (string.capitalize)
    Обнаружена орфографическая ошибка в слове capitilize и capitalize строка 10 и строка 15

Тест #2 на удаление пробелов:
    При проведении автотеста, Pytest выдал ошибку
    в функции while (string.startswith(whitespace)):
    которая предполагает входные данные типа str, в нашем случае
    мы подаем значение None, что не является str. строка 32

Тест #3 на разделение:
    При проведении автотеста, Pytest выдал ошибку
    в функции def test_to_list_negative(string_utils):
    которая предполагает входные данные типа str, в нашем случае
    мы подаем значение None, что не является str. строка 46

Тест #5 на удаление подстрок:
    При проведении автотеста, Pytest выдал ошибку
    в функции def test_to_list_negative(string_utils):
    которая предполагает входные данные типа str, в нашем случае
    мы подаем значение None, что не является str. строка 46

